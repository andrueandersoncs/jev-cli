# Original Idea

Turn this into a CLI https://docs.typesafe.ai/introduction

Ideal API is something like this:

`jev --state "./some-state.json" --prompt "what do I do next?" --choices "run" "hide" "fight" "entice"`